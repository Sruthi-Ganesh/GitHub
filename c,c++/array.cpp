#include<iostream>
#include<string.h>
using namespace std;
int main()
{
	string name[100];
	int score;
	

	
	fgets(name,100,stdin);
	for(int i=0;i<strlen(name);i++)
	{
		if(isdigit(name[i])
		{
			score = isdigit[i];
		}
	}

return 0;
}
